# ── Core API ──────────────────────────────────────────────────────────────────
fastapi
uvicorn
celery
redis
supabase
slowapi
beautifulsoup4
httpx>=0.27.0
tenacity>=8.0.0
pydantic
python-jose[cryptography]

# ── Motor A: Scrapling (reemplaza playwright + playwright-stealth) ─────────────
# scrapling[all] incluye TODAS las dependencias de browser fingerprinting:
#   - DynamicFetcher: Chromium gestionado via Playwright con patches anti-detección  
#   - StealthyFetcher: Cloudflare bypass automático (solve_cloudflare=True para Lider)
#   - Extras: curl_cffi, msgspec, browserforge, rebrowser-playwright, patchright
scrapling[all]>=0.4.0

# Playwright lo instala Scrapling internamente, pero lo declaramos para que
# la imagen Docker pueda hacer `playwright install chromium` explícitamente.
playwright

# ── Motor B: LLM Universal Extractor ──────────────────────────────────────────
google-generativeai>=0.8.0      # Gemini 2.5 Flash-Lite SDK
groq>=0.9.0                     # Groq Llama 3.3 fallback
trafilatura>=1.8.0               # HTML → clean text (reduce LLM tokens)
duckduckgo-search>=6.0.0         # Modalidad B — descubrimiento de competidores (SRS §MB-02)
rapidfuzz>=3.0.0

